---
title: 客户端接入
---

# 客户端接入

你可以把 FlowVision 注册为 MCP Server，然后在支持 MCP 的客户端中直接调用它的工具。

## 基本步骤

1. 确认 FlowVision MCP 服务可用
2. 在客户端配置 MCP Server
3. 重启客户端并验证工具列表

## 配置时要确认的内容

- Server 启动命令
- 工作目录
- 环境变量
- 与 FlowVision 主程序的连接方式

## 验证方法

接入后先执行一次只读工具，例如获取当前图状态或列出可用工具，确保链路可用后再尝试写操作。
